# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-O2 -pipe -march=native"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C

MAKEOPTS="-j8"

# required systemwide USE flags
USE="${USE} \
# languages
l10n_en l10n_en-GB l10n_ja l10n_zh-CN \
# this laptop
synaptics opengl thunderbolt nvme uefi tpm \
# kernel
hardened \
# elogind
elogind -systemd dbus udev polkit \
# x11
X xvmc xinerama cjk vulkan hwaccel acl ibus \
# network
wifi networkmanager bluetooth \
# pipewire
pipewire alsa pulseaudio gstreamer cdda \
# gui
gtk gtk3 \
# clang <3
pgo lto thinlto clang \
# printers
cups \
# spyware
-telemetry \
#end
"

INPUT_DEVICES="synaptics libinput"

GRUB_PLATFORMS="efi-64"

VIDEO_CARDS="intel"

ACCEPT_LICENSE="*"

#GENTOO_MIRRORS="http://gentoo.mirrors.tera-byte.com/ rsync://mirrors.tera-byte.com/gentoo ftp://mirror.csclub.uwaterloo.ca/gentoo-distfiles/ https://mirror.csclub.uwaterloo.ca/gentoo-distfiles/ http://mirror.csclub.uwaterloo.ca/gentoo-distfiles/ rsync://mirror.csclub.uwaterloo.ca/gentoo-distfiles http://www.gtlib.gatech.edu/pub/gentoo rsync://rsync.gtlib.gatech.edu/gentoo https://gentoo.osuosl.org/ http://gentoo.osuosl.org/ http://gentoo.mirrors.pair.com/ https://mirrors.rit.edu/gentoo/ http://mirrors.rit.edu/gentoo/ ftp://mirrors.rit.edu/gentoo/ rsync://mirrors.rit.edu/gentoo/ http://gentoo.mirrors.tds.net/gentoo http://gentoo-mirror.flux.utah.edu/"

ALSA_CARDS="maestro3 hdsp hdspm ymfpci"

CPU_FLAGS_X86="aes avx avx2 f16c fma3 mmx mmxext pclmul popcnt rdrand sse sse2 sse3 sse4_1 sse4_2 ssse3"

QEMU_SOFTMMU_TARGETS="arm x86_64 aarch64 aarch64_be"

QEMU_USER_TARGETS="arm x86_64 aarch64 aarch64_be"
